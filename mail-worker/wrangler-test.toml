# 安全提示:请勿在本文件硬编码数据库ID/JWT密钥,避免随仓库泄露。
# 下面使用环境变量占位符,部署前先注入:
#   export D1_DATABASE_ID="你的D1数据库ID"
#   export JWT_SECRET="你的随机密钥"	# 也可用 wrangler secret put jwt_secret 注入(代码读取的是 c.env.jwt_secret)
name = "test-mail"
main = "src/index.js"
compatibility_date = "2025-06-04"
keep_vars = true

[observability]
enabled = true

[[d1_databases]]
binding = "db"			#d1数据库绑定名默认不可修改
database_name = "test-email"		#d1数据库名字
database_id = "${D1_DATABASE_ID}"		#d1数据库id,通过环境变量 D1_DATABASE_ID 注入

[[kv_namespaces]]
binding = "kv"			#kv绑定名默认不可修改
id = "9be10cd058e04c55b526f8c0c116f50c"					#kv数据库id

#(可选)
#[[r2_buckets]]
#binding = "r2"			#r2对象存储绑定名默认不可修改
#bucket_name = "test-email"		#r2对象存储桶的名字

[ai]
binding = "ai"

[assets]
binding = "assets"		#静态资源绑定名默认不可修改
directory = "./dist"	#前端vue项目打包的静态资源存放位置,默认dist
not_found_handling = "single-page-application"
run_worker_first = true

[triggers]
crons = ["0 * * * *"]	#每小时执行；resetDaySendCount 仅在 UTC 0 点生效


[vars]
ai_model = ""			#ai模型,不填默认使用@cf/meta/llama-3.1-8b-instruct-fast
analysis_cache = false			#是否开启分析数据缓存
orm_log = false			#是否sql日志
domain = ["example.com"]				#邮件域名可可配置多个 示例: ["example1.com","example2.com"]
admin = "admin@example.com"				#管理员的邮箱	示例: admin@example.com
jwt_secret = "${JWT_SECRET}"			#jwt令牌的密钥,通过环境变量 JWT_SECRET 注入,勿硬编码

[build]
command = "pnpm --prefix ../mail-vue install && pnpm --prefix ../mail-vue run build"
